sudo: required
services:
  - docker

before_install:
  - docker build -t mydockeridlili/multicontainer -f Dockerfile.dev .

script:
  - docker run -e CI=true mydockeridlili/react-test npm test

after_success:
  - docker build -t mydockeridlili/multicontainer ./client
  - docker build -t mydockeridlili/multicontainer ./nginx
  - docker build -t mydockeridlili/multicontainer ./server
  - docker build -t mydockeridlili/multicontainer ./worker 
  # Login in the docker CLI
  - echo "$DOCKER_PASSWOER" | docker login -u "DOCKER_ID" --password-stdin
  - docker push mydockerid/multicontainer-client
  - docker push mydockerid/multicontainer-nginx
  - docker push mydockerid/multicontainer-server
  - docker push mydockerid/multicontainer-worker
